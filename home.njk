---
layout: layouts/base.njk
section: home
permalink: "/index.html"
---

<h2 class="p--2 pb--0">exhibitions</h2>

<div class="home-gallery p--2 mb--2 scalloped">
  {% set projectGroupList = collections.projectGroup | limit(settings.theme.theme_features.item_output_limit or 48) %}
  {% include "components/projectGroupList.njk" %}

</div>


<section class="project-home-list p--2">
  <h2>projects</h2>
    {% set projectList = collections.projects | projectGroupFilter("work/unassigned.md") | limit(settings.theme.theme_features.item_output_limit or 4) %}
    {# the projectList component has its own conditional rendering logic, so we don't have to hide it #}
     {% include 'components/projectList.njk' %}
</section>


{# Appended to <head> during build #}
{% include "components/opengraph.njk" %}